name: Build and Deploy to AKS

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      # Checkout source code
      - name: Checkout repository
        uses: actions/checkout@v4

      # Set up Python and cache pip dependencies (if needed by scripts)
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install Python dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      # Set up Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Authenticate with GitHub Container Registry
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GHCR_PAT || secrets.GITHUB_TOKEN }}

      # Build and push the Docker image
      - name: Build and push Docker image
        run: |
          docker buildx build \
            --platform linux/amd64 \
            -t ghcr.io/greenbowalabama/launch-line:latest \
            --push .

      # Set up kubectl
      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      # Login to Azure and get AKS credentials
      - name: Azure login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Set kube context
        run: |
          az aks get-credentials \
            --resource-group launch-labs-dev-rg \
            --name launch-labs-aks \
            --overwrite-existing

      # Deploy MediaMTX to AKS (custom script)
      - name: Deploy MediaMTX to AKS
        run: |
          chmod +x scripts/deploy-mediamtx.sh
          ./scripts/deploy-mediamtx.sh dev

      # Trigger rollout of MediaMTX
      - name: Patch MediaMTX deployment to trigger rollout
        run: |
          kubectl patch deployment mediamtx -n mediamtx -p \
            "{\"spec\":{\"template\":{\"metadata\":{\"annotations\":{\"build-timestamp\":\"$(date +'%s')\"}}}}}"

      # Deploy application
      - name: Deploy application to AKS
        run: |
          kubectl apply -f k8s/deployment.yaml
          kubectl apply -f k8s/service.yaml

      # Restart application
      - name: Restart application
        run: |
          kubectl rollout restart deployment launch-labs-app
